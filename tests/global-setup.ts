// The exact file the README prescribes to consumers, pointed at the library source.
// Zero arguments: the integration run proves the documented defaults end-to-end.
import { createGlobalSetup } from "../src/global-setup.js";

export default createGlobalSetup();
